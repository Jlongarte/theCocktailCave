# Aseguramos que los comandos de Netlify sean los estándar de Vite
[build]
  command = "npm run build"
  publish = "dist"

# Redirección obligatoria para React Router (Evita el error 404 al refrescar)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200